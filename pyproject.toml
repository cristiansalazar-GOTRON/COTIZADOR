[project]
name = "cotizador"
version = "0.1.0"
dependencies = [
    "customtkinter",
    "pillow>=12.1.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "pyinstaller",
]
